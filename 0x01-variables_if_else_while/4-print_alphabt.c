#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * more headers goes there 
 * main -still going on
 * betty style doc for function main goes there 
 */
int main(void)                                                      
{	char c;
	c = 'a';
	while (c <= 'z')
	{	if((c !=m'e'||c !='q') && c <= 'z')
			putchar(c);
		c++;}
	putchar('\n');
	return (0);
}
